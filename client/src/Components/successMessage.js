import React, {useState} from 'react';


const SuccessMessage = ({active,setActive, headerMessage, secondeMessage, setRewardEarned}) => {

	/*const onClickClose = (event) =>{
	setActive(false);
	cleanAtClosing();
	}

	const cleanAtClosing = ()=>{
	setRewardEarned([]);
	}
	
	const state = active === true ? 'active' : 'hidden';

	////////

	<div className={`ui positive message ${state}`} style = {{textAlign:'center' , marginTop:'30px'}}>
		  	<i className="close icon" onClick = {onClickClose}></i>
	  		<div className="header green">
	   			{headerMessage}
			</div>
		  	<p>{secondeMessage}</p>
		  	{value}
		</div>*/

	return (
		<div>
		test
		</div>
		
	);
	

}

export default SuccessMessage;